
Lift Arm and Open Wrist at initializing autonomous mode
    Command : autoArm.moveArm

Move arm to foundation Level
    Command: autoArm.moveArm_aboveFoundationLevel();

Run Chassis forward and backward till :
    distance (+ve for forward - for backward), strafe (1 for right -1 for left)
    distance calibaration accurate to inches at 0.1power for 2-10 inches, at 0.25 rpm for 10-60 inches
    Command: autoChassis.runFwdBackLeftRight(Distance, strafe, power)

Turn 90 degrees clockwise(1) or anticlockwise(-1) at power
    Command: autoChassis.turnby90degree(clockwise / anticlockwise, pwoer);

Run chassis left till Red/Blue color is detected on right color sensor or max distance is reached at power
     - till max_distance (+ for forward - for backward)
     - strafe direction 0 for forward / backward, 1 for right, -1 for left
    Command: autoChassis.runTill_ChassisRightColorSensorIsRed(max_distance, strafeDirection, power);
    Command: autoChassis.runTill_ChassisRightColorSensorIsBlue(max_distance, strafeDirection, power);
    Command: autoChassis.runTill_ChassisLeftColorSensorIsRed(max_distance, strafeDirection, power);
    Command: autoChassis.runTill_ChassisLeftColorSensorIsBlue(max_distance, strafeDirection, power);

Run forward till front left chassis touch sensor is pressed or max_distance is reached.
    Command : runFwdTill_frontleftChassisTouchSensor_Pressed(double max_stop_distance, double power)

Open Grip / Close Grip
    Command : autoIntake.openGrip() or autoIntake.closeGrip