
Move arm to foundation Level
    Command: hzArm.moveArm_aboveFoundationLevel();

Run Chassis till distance
    distance (+ for forward - for backward), strave right : direction = -Math.PI/2, strave left : direction = Math.PI/2
    distance calibaration forward/backward : 9.8" vs 10" input. strave left/right : 9" vs 10" input 13" vs 15" input
    Command: hzChassis.runDistance(Distance, -Math.PI/2, 0, 0.25);

Turn 90 degrees clockwise at power
    Command: hzChassis.runDistance(0, 0, -Math.PI/2, power);

Turn 90 degrees anti-clockwise at power
    Command: hzChassis.runDistance(0, 0, Math.PI/2, power);

Run chassis left till Red color is detected on right color sensor or max distance is reached at power
     - till max_distance (+ for forward - for backward)
     - strave direction 0 for forward / backward, 1 for right, -1 for left
    Command: hzChassis.StraveTill_ChassisRightColorSensorIsRed(max_distance, strave direction, power);

Run forward till front left chassis touch sensor is pressed or max_distance is reached.
    Command : runFwdTill_frontleftChassisTouchSensor_Pressed(double max_stop_distance, double power)

